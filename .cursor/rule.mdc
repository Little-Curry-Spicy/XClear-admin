---
alwaysApply: true
---
一个基于 Vue3 + Vite + TypeScript 构建的现代化后台管理系统，使用 Tailwind CSS 和 **PrimeVue** 组件库，完全适配移动端，支持主题切换。 风格是 `shadcn` 黑白简约分格，你帮我生成的所有组件都要遵循这个原则，尽量使用 `PrimeVue`组件的原始样式，不需要特别添加样式去改变他的风格，我要保证风格统一，可以适当的添加一些提升用户体验的动画，使用 `pnpm` 作为包管理器,创建文件时要考虑移动端