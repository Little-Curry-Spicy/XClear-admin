# 接口基础地址（生产环境改为实际后端地址）
# VITE_API_BASE_URL=https://api.example.com

# 是否启用从后端拉取菜单（可选，需后端提供 GET /api/menu）
# VITE_USE_MENU_API=false
# 系统名称（用于顶栏/侧栏展示，需 VITE_ 前缀才能在代码中访问）
VITE_SYSTEM_NAME=XClear Admin
# sidebar/ topnav
VITE_LAYOUT_MODE=sidebar
# auto/ light/ dark
VITE_THEME_MODE=auto